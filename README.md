# tettou771AddonsDeployTool

For openFrameworks addon installer.
This is deploy script that clone into addons folder.

# Usage

Clone this into addons.

```
OF_DIR/addons/tettou771AddonsDeployTool
```

Run

```
$ cd tettou771AddonsDeployTool
$ bash deploy.sh
``` 
